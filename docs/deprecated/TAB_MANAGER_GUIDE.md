# Tab Manager Guide

The Sage Utils Tab Manager Guide has been moved to the OKF bundle.

See `okf/docs/tab_manager_guide.md` for the current canonical guide and usage examples.

This file now serves as a backward-compatible pointer to the structured OKF documentation.

> Note: The OKF `ui` and `docs` subbundles are the primary sources for UI and feature documentation. Legacy docs under `docs/` are retained only as redirects and quick references.
